<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:background="@drawable/background"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <TextView android:text="@string/App_hello_message"
        android:id="@+id/testId111"
        android:layout_margin="10dp"
        android:textColor="#ff061b64"
        android:paddingTop="10dp"
        android:layout_marginBottom="40dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <ImageView
        android:id="@+id/test_image"
        android:src="@drawable/images"
        android:scaleType="fitEnd"
        android:adjustViewBounds="true"
        android:layout_below="@+id/testId111"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="visible"
        />


     <Button
         android:id="@+id/buttonId1"
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"
         android:layout_below="@+id/test_image"
         android:text="click to start XYZ activity"
         android:onClick="HelloXYZ"
         />

    <EditText
        android:id="@+id/search123"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/buttonId1"
        android:inputType="text"
        android:imeOptions="actionSend"
        />


</RelativeLayout>
